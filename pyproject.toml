[tool.black]
line-length = 88
target-version = ["py38"]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --cov=labelshift -n auto"
testpaths = [
    "tests",
]

[tool.coverage.report]
fail_under = 73.00

